{
  "name": "server",
  "version": "1.0.0",
  "description": "This is a simplified version that uses an in-memory data store (an array) to store the data for the CRUD operations. In a real-world scenario, you would typically use a database or external data source.",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "abhiman ranaweera",
  "license": "ISC",
  "dependencies": {
    "apollo-server-express": "^3.12.0",
    "express": "^4.18.2",
    "graphql": "^16.7.1"
  }
}
